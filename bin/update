#!/usr/bin/env ruby

puts "Update to the latest RubyGems version"
  system "gem update --system"

puts "Update Atom packages"
  system "yes | apm upgrade"

puts "Update brew"
  system "brew update && brew upgrade && brew cleanup"

puts "Updae global npm packages"
  system "npm update npm -g"
